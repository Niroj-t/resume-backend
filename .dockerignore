# Build artefacts
obj/
bin/

# Secrets — never bake into the image
.env
appsettings.Development.json
**/secrets.json

# Dev tooling
.vs/
.vscode/
*.user

# Uploaded files (handled via Docker volume at runtime)
wwwroot/uploads/*
!wwwroot/uploads/.gitkeep
